apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
data:
  mosquitto.conf: |
    allow_anonymous true
    listener 1883
    persistence true
    persistence_location /mosquitto/data/
