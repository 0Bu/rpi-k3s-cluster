apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: deconz-vnc-password-{{ .Release.Name }}
spec:
 template:
   metadata:
     name: deconz-vnc-password-{{ .Release.Name }}
 encryptedData:
    vnc-password: {{ .Values.vnc.password }}

