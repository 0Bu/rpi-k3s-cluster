apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana
  annotations:
    helm.sh/resource-policy: keep
spec:
  storageClassName: ""
  volumeName: grafana
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
