apiVersion: v1
kind: Secret
metadata:
  name: telegraf-ds-influxdb-token
  annotations:
    helm.sh/resource-policy: keep
data:
  INFLUXDB_TOKEN: {{ required "run with --set token=ABCDE" .Values.token | b64enc }}

